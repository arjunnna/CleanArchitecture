# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CleanArchictureSample' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  pod 'Alamofire', '~> 4.7'
  pod 'Moya', '~> 13.0.0'
  pod 'AlamofireImage', '~> 3.5'
  pod 'SkeletonView'
  pod 'DZNEmptyDataSet'

  # Pods for CleanArchictureSample

  target 'CleanArchictureSampleTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CleanArchictureSampleUITests' do
    # Pods for testing
  end

end
